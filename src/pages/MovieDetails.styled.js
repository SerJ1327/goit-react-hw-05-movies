import styled from 'styled-components';

const StyledHeroWrapper = styled.div`
  display: flex;
  gap: 10px;
  margin-top: 5px;
  margin-bottom: 10px;
`;

const StyledPosterImg = styled.img`
  width: 300px;
`;

const StyledGenresDiv = styled.div`
  display: flex;
`;

const StyledInfoWrapper = styled.div`
  border-top: 1px solid grey;
  gap: 4px;
`;
export {
  StyledHeroWrapper,
  StyledPosterImg,
  StyledInfoWrapper,
  StyledGenresDiv,
};
